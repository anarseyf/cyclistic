## Cyclistic

### The Data

```{r ggplot attempt}
# library(ggplot2)
d <- read.csv("avg-duration-by-weekday.csv")
head(d)
ggplot(data = d, aes(x = WEEKDAY, y = AVG_DURATION, color = is_member)) +
  geom_point() +
  facet_wrap(~MONTH)
```